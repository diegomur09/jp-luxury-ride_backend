{
  "name": "lux-ride-lambda-functions",
  "version": "1.0.0",
  "description": "AWS Lambda functions for JP Luxury Ride backend",
  "main": "index.js",
  "scripts": {
    "build": "npm install --production",
    "package": "zip -r function.zip ."
  },
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "stripe": "^14.8.0",
    "squareup": "^1.0.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "aws-sdk": "^2.1400.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
