{
  "name": "payments-lambda",
  "version": "1.0.0",
  "description": "Payment processing Lambda function supporting Stripe and Square",
  "main": "index.js",
  "scripts": {
    "test": "jest --passWithNoTests",
    "lint": "eslint ."
  },
  "dependencies": {
    "stripe": "^14.5.0",
    "square": "^43.2.0",
    "@supabase/supabase-js": "^2.38.0",
    "@aws-sdk/client-dynamodb": "^3.659.0",
    "@aws-sdk/lib-dynamodb": "^3.659.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  }
}
